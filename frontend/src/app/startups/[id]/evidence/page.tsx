import{PageHead}from"@/components/PageHead";import{ActionForm}from"@/components/ActionForm";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <><PageHead kicker={`Candidate #${id} / Evidence`} title="Complete the diligence record." copy="Only the recorded founder wallet can attach the profile, repository, and market sources used by validators."/><section className="section compact"><ActionForm mode="evidence" startupId={id}/></section></>}
