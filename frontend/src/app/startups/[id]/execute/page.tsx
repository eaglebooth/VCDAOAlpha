import{PageHead}from"@/components/PageHead";import{ActionForm}from"@/components/ActionForm";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <><PageHead kicker={`Candidate #${id} / Settlement`} title="Execute the seed investment." copy="This manager action releases the reserved GEN to the authenticated founder and creates a permanent portfolio position."/><section className="section compact"><ActionForm mode="execute" startupId={id}/></section></>}
