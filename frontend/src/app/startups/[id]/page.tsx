import{PageHead}from"@/components/PageHead";import{StartupDetail}from"@/components/StartupDetail";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <><PageHead kicker={`Candidate #${id}`} title="One record. Every decision gate." copy="Sync the candidate to inspect its founder, evidence, diligence, offer status, and available next actions."/><StartupDetail id={id}/></>}
