import{PageHead}from"@/components/PageHead";import{ActionForm}from"@/components/ActionForm";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <><PageHead kicker={`Candidate #${id} / Offer`} title="Reserve capital against published terms." copy="Only the manager can issue an offer, and only after an APPROVED decision with sufficient unreserved treasury capital."/><section className="section compact"><ActionForm mode="offer" startupId={id}/></section></>}
